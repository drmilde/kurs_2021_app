import 'package:flutter/material.dart';

class JanTorstenMScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Jan-Torstens Seite"),
      ),
      body: Container(
        child: Text("Jan-Torsten"),
      ),
    );

  }
}
