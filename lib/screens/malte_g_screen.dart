import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class MalteGScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Maltes Steckbrief"),
      ),  // AppBar
      body: Container(
        child: Text("lmao"),
      ),
    );
  }
}